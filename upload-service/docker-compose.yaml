
name: "upload"
services:
  service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3006:3006"
    env_file:
      - ./.env
    networks:
      - micro-network
      
networks:
  micro-network:
    external: true
    name: microservices-net
