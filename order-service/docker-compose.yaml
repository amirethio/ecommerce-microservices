name: "order"
services:
  service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3004:3004"
    env_file:
      - ./.env
    networks:
      - micro-network

networks:
  micro-network:
    external: true
    name: microservices-net
