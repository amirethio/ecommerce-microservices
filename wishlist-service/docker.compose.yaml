version: "3.8"

services:
  wishlist-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    env_file:
      - .env
    environment:
      - NODE_ENV=development
      - PORT=3000
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/wishlist
    depends_on:
      - postgres
    networks:
      - wishlist-network

  postgres:
    image: postgres:15.4
    ports:
      - "5434:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=wishlist
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - wishlist-network

networks:
  wishlist-network:
    driver: bridge

volumes:
  postgres-data:
