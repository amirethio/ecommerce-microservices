name: "coupon"
services:
  service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3011:3011"
    env_file:
      - ./.env
    networks:
      - micro-network

networks:
  micro-network:
    external: true
    name: microservices-net
