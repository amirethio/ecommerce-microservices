
name: "user"
services:
  service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    env_file:
      - ./.env
    networks:
      - micro-network

networks:
  micro-network:
    external: true
    name: microservices-net
